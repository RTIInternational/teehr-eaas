# Coordinator configuration template
coordinator=${TRINO_COORDINATOR}
node-scheduler.include-coordinator=${TRINO_INCLUDE_COORDINATOR}
http-server.http.port=8080
discovery.uri=${TRINO_DISCOVERY_URI}
query.max-memory=${TRINO_MAX_MEMORY}
query.max-memory-per-node=${TRINO_MAX_MEMORY_PER_NODE}